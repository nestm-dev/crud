---
"@nestm/crud": minor
---

Emit versioned CRUD filter metadata and add an optional Swagger UI condition
builder with field, operator, value, and add/remove controls while preserving
the existing bracket-query HTTP contract.
